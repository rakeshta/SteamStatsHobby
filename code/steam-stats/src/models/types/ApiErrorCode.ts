/** API error codes that can be translated to error messages in the UI. */
export enum ApiErrorCode {
  /** Other - Catch all error code for un-handled errors. */
  Other = 'Other',

  /** InvalidSteamId - The steam ID provided was not valid. */
  InvalidSteamId = 'InvalidSteamId',
}
